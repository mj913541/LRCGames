// Add/merge this into your Firestore rules.
// This protects the shared lesson planner document so only your two approved
// authenticated accounts can read or write it.

match /plannerDashboard/lessonPlanning {
  allow read, write: if request.auth != null
    && request.auth.token.email in [
      "malbrecht@sd308.org",
      "malbrecht3317@gmail.com"
    ];
}
